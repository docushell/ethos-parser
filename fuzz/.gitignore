# Build output and libFuzzer's own working state.
#
# `corpus/` is generated by `seed-corpus.sh` and then grown by libFuzzer, so it is working state,
# not a source. The committed seeds live in `seeds/` — see that script's header for why the two
# are kept apart.
/target
/corpus
/artifacts
/coverage
